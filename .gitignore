# Secrets — never commit real credentials
.env
.env.local

# Dependencies
node_modules/

# Logs / debug
*.log
npm-debug.log*

# Verification logs - contain student names, matricules and Discord IDs.
# Kept locally, never committed.
used-ids.json
cyber-verif-logs.json
